# Valores para desenvolvimento local.
# NÃO use esses valores em produção.

# Secret para assinar/verificar o cookie result_token.
# Em produção, defina um valor aleatório forte via variável de ambiente.
RESULT_SECRET=dev-test-secret
